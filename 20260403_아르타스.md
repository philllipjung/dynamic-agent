# Arthas Manager 상세 분석

**분석일자:** 2026-04-03
**모듈:** arthas-manager
**버전:** 1.0.0

---

## 📋 목차

1. [개요](#개요)
2. [모듈 구조](#모듈-구조)
3. [의존성](#의존성)
4. [ArthasManager 상세 분석](#arthasmanager-상세-분석)
5. [ArthasHttpClient 상세 분석](#arthashttpclient-상세-분석)
6. [HTTP API 명세](#http-api-명세)
7. [사용 예시](#사용-예시)
8. [트러블슈팅](#트러블슈팅)

---

## 개요

### Arthas란?

**Arthas**는 알리바바에서 개발한 Java 진단 도구로, 실행 중인 JVM을 동적으로 분석하고 문제를 해결할 수 있는 오픈소스 프로젝트입니다.

### 주요 기능
- **클래스 로딩 및 언로딩**: 런타임에 클래스 수정
- **메서드 추적**: 메서드 호출 스택 및 실행 시간 분석
- **파라미터 감시**: 메서드 파라미터 및 반환값 확인
- **클래스 검색**: 로드된 클래스 및 메서드 검색
- **스레드 분석**: 스레드 상태 및 덤프 확인
- **HTTP API**: 원격 제어를 위한 REST API 제공

---

## 모듈 구조

```
arthas-manager/
├── pom.xml                                    # Maven 설정
└── src/main/java/com/javaagent/arthas/
    ├── ArthasManager.java                     # Arthas 관리자 (메인)
    └── ArthasHttpClient.java                  # HTTP API 클라이언트
```

---

## 의존성

### pom.xml 분석

```xml
<dependencies>
    <!-- 공통 모듈 -->
    <dependency>
        <groupId>com.javaagent</groupId>
        <artifactId>agent-commons</artifactId>
    </dependency>

    <!-- Arthas 코어 -->
    <dependency>
        <groupId>com.taobao.arthas</groupId>
        <artifactId>arthas-agent</artifactId>
    </dependency>
    <dependency>
        <groupId>com.taobao.arthas</groupId>
        <artifactId>arthas-core</artifactId>
    </dependency>
    <dependency>
        <groupId>com.taobao.arthas</groupId>
        <artifactId>arthas-boot</artifactId>
    </dependency>

    <!-- HTTP 통신 -->
    <dependency>
        <groupId>org.springframework</groupId>
        <artifactId>spring-web</artifactId>
        <version>6.1.5</version>
    </dependency>

    <!-- JSON 처리 -->
    <dependency>
        <groupId>org.json</groupId>
        <artifactId>json</artifactId>
        <version>20240303</version>
    </dependency>
</dependencies>
```

### 의존성 목록

| 라이브러리 | 버전 | 용도 |
|-----------|------|------|
| agent-commons | 1.0.0 | 공통 상수 및 설정 |
| arthas-agent | (managed) | Arthas 에이전트 |
| arthas-core | (managed) | Arthas 코어 기능 |
| arthas-boot | (managed) | Arthas 부트스트랩 |
| spring-web | 6.1.5 | RestTemplate (HTTP 클라이언트) |
| org.json | 20240303 | JSON 파싱 |
| slf4j-api | 2.0.9 | 로깅 |

---

## ArthasManager 상세 분석

### 클래스 개요

```java
public class ArthasManager {
    // 세션 관리
    private static final Map<String, ArthasSession> activeSessions;
    private static final Map<String, String> pidToSessionId;

    // Arthas 설정
    private static String arthasHome;
    private static String tunnelServerHost;  // 기본: 127.0.0.1
    private static int tunnelServerPort;      // 기본: 8563

    // HTTP 통신
    private static final RestTemplate restTemplate;

    // 비동기 watch
    private static final ExecutorService watchExecutor;
    private static final Map<String, JSONObject> watchResults;
    private static final Map<String, String> watchStatus;
}
```

---

### 1. 초기화 및 설정

#### 1.1 정적 초기화 블록

```java
static {
    // RestTemplate 설정 (긴 타임아웃)
    SimpleClientHttpRequestFactory factory = new SimpleClientHttpRequestFactory();
    factory.setConnectTimeout(60000);  // 60초
    factory.setReadTimeout(60000);     // 60초 (watch는 시간이 걸림)
    restTemplate = new RestTemplate(factory);

    // 터널 서버 설정
    tunnelServerHost = System.getProperty(
        AgentConstants.PROP_ARTHAS_HOST,
        AgentConstants.DEFAULT_ARTHAS_HOST
    );  // 127.0.0.1

    tunnelServerPort = Integer.parseInt(System.getProperty(
        AgentConstants.PROP_ARTHAS_PORT,
        String.valueOf(AgentConstants.DEFAULT_ARTHAS_PORT)
    ));  // 8563
}
```

#### 1.2 터널 서버 설정

```java
public static void configureTunnelServer(String host, int port) {
    if (host == null || host.isEmpty()) {
        throw new IllegalArgumentException("arthas.tunnel.server.host must be configured");
    }
    if (port <= 0 || port > 65535) {
        throw new IllegalArgumentException("arthas.tunnel.server.port must be between 1 and 65535");
    }

    tunnelServerHost = host;
    tunnelServerPort = port;

    System.out.println("[ArthasManager] Tunnel server configured: " + host + ":" + port);
}
```

---

### 2. JVM 부착 (Attach)

#### 2.1 클래스 이름으로 부착

```java
public static String attachByClassName(String className) {
    try {
        // 1. PID 찾기
        String pid = findPidByClassName(className);
        if (pid.startsWith("ERROR")) {
            return pid;
        }

        // 2. PID로 부착
        return attachByPid(pid);
    } catch (Exception e) {
        return "ERROR: " + e.getMessage();
    }
}
```

#### 2.2 PID로 부착

```java
public static String attachByPid(String pid) {
    // 이미 부착된 경우
    if (activeSessions.containsKey(pid)) {
        return "Arthas already attached to PID: " + pid;
    }

    try {
        // 1. Arthas 존재 확인
        ensureArthasAvailable();
        File arthasBoot = new File(arthasHome, "arthas-boot.jar");

        // 2. arthas-boot.jar 실행 (HTTP API 활성화)
        String os = System.getProperty("os.name").toLowerCase();
        ProcessBuilder pb;

        if (os.contains("win")) {
            // Windows
            pb = new ProcessBuilder("cmd.exe", "/c",
                "start /B java -jar \"" + arthasBoot.getAbsolutePath() + "\"" +
                " --target-ip " + tunnelServerHost +
                " --http-port " + tunnelServerPort + " " + pid);
        } else {
            // Unix/Mac
            pb = new ProcessBuilder("bash", "-c",
                "java -jar " + arthasBoot.getAbsolutePath() +
                " --target-ip " + tunnelServerHost +
                " --http-port " + tunnelServerPort + " " + pid + " &");
        }

        pb.redirectErrorStream(true);
        pb.start();

        // 3. HTTP API 시작 대기
        Thread.sleep(3000);

        // 4. 세션 생성 및 등록
        String sessionId = "arthas-" + pid + "-" + System.currentTimeMillis();
        ArthasSession session = new ArthasSession(sessionId, pid);
        session.start();

        activeSessions.put(pid, session);
        pidToSessionId.put(pid, sessionId);

        return "SUCCESS: Arthas attached to PID " + pid +
               " (Session: " + sessionId + "), HTTP API on port " + tunnelServerPort;

    } catch (Exception e) {
        e.printStackTrace();
        return "ERROR: Failed to attach Arthas - " + e.getMessage();
    }
}
```

**주요 포인트:**
- `--target-ip`: Arthas HTTP API가 리스닝할 IP
- `--http-port`: HTTP API 포트 (기본 8563)
- 백그라운드 실행 (`&` 또는 `start /B`)
- 3초 대기 후 HTTP API 사용 가능

---

### 3. HTTP API 통신

#### 3.1 명령 실행

```java
public static String executeTunnelApiCommand(String pid, String command) throws IOException {
    try {
        // 1. API URL 생성
        String url = String.format("http://%s:%d/api", tunnelServerHost, tunnelServerPort);

        // 2. JSON 요청 본문 생성 (공식 API 형식)
        JSONObject requestBody = new JSONObject();
        requestBody.put("action", "exec");
        requestBody.put("command", command);
        requestBody.put("execTimeout", "60000");

        // 3. HTTP 헤더 설정
        HttpHeaders headers = new HttpHeaders();
        headers.setContentType(MediaType.APPLICATION_JSON);

        HttpEntity<String> entity = new HttpEntity<>(requestBody.toString(), headers);

        System.out.println("[ArthasManager] API Request: " + requestBody.toString());

        // 4. HTTP POST 실행
        ResponseEntity<String> response = restTemplate.exchange(
            url, HttpMethod.POST, entity, String.class
        );

        String responseBody = response.getBody();
        if (responseBody == null) {
            return "{\"error\": \"No response from server\"}";
        }

        System.out.println("[ArthasManager] API Response: " + responseBody);

        return responseBody;

    } catch (Exception e) {
        return "{\"error\": \"API call failed: " + e.getMessage() + "\"}";
    }
}
```

**HTTP API 요청 형식:**
```json
POST http://127.0.0.1:8563/api
Content-Type: application/json

{
  "action": "exec",
  "command": "watch com.example.MyClass myMethod '{params}' -n 1",
  "execTimeout": "60000"
}
```

**HTTP API 응답 형식:**
```json
{
  "state": "SUCCEEDED",
  "body": {
    "results": [
      {
        "type": "watch",
        "value": "params[0]=\\"userId\\", params[1]=\\"sessionId\\""
      }
    ]
  }
}
```

---

### 4. Watch 명령 (파라미터 추출)

#### 4.1 파라미터와 함께 Watch

```java
public static JSONObject watchWithParameters(String className, String methodName, int limit) {
    try {
        String pid = getFirstAvailablePid();
        if (pid.startsWith("ERROR") || pid.isEmpty()) {
            return new JSONObject().put("error", "No running JVM found");
        }

        // 1. watch 명령 생성
        String watchCmd = String.format("watch %s %s params -n %d -x 1",
            className, methodName, limit);

        System.out.println("[ArthasManager] Executing watch: " + watchCmd);

        // 2. HTTP API로 실행
        String result = executeTunnelApiCommand(pid, watchCmd);

        // 3. JSON 응답 파싱
        JSONObject response = new JSONObject(result);

        // 4. 에러 확인
        if (response.has("error")) {
            return response;
        }

        // 5. 상태 확인
        String state = response.optString("state", "UNKNOWN");
        if (!"SUCCEEDED".equals(state)) {
            return new JSONObject().put("error", "Command execution failed: " + state);
        }

        // 6. 파라미터 파싱
        JSONArray parameters = new JSONArray();
        if (response.has("body")) {
            JSONObject body = response.getJSONObject("body");
            if (body.has("results")) {
                JSONArray results = body.getJSONArray("results");

                for (int i = 0; i < results.length(); i++) {
                    JSONObject resultItem = results.getJSONObject(i);

                    if ("watch".equals(resultItem.optString("type"))) {
                        String value = resultItem.optString("value", "");
                        parameters = parseWatchValue(value);
                    }
                }
            }
        }

        // 7. 결과 반환
        JSONObject resultJson = new JSONObject();
        resultJson.put("success", true);
        resultJson.put("className", className);
        resultJson.put("methodName", methodName);
        resultJson.put("parameters", parameters);
        resultJson.put("parameterCount", parameters.length());
        resultJson.put("rawResponse", response);

        System.out.println("[ArthasManager] Extracted " + parameters.length() + " parameters");

        return resultJson;

    } catch (Exception e) {
        e.printStackTrace();
        return new JSONObject().put("error", e.getMessage());
    }
}
```

#### 4.2 Watch 값 파싱

```java
private static JSONArray parseWatchValue(String value) {
    JSONArray parameters = new JSONArray();

    if (value == null || value.isEmpty()) {
        return parameters;
    }

    try {
        // 1. 이스케이프 문자 제거
        value = value.replace("\\\"", "\"");
        value = value.replace("\\\\", "\\");

        // 2. params[N]=pattern 파싱
        // 예: params[0]="userId", params[1]="sessionId"
        java.util.regex.Pattern pattern = java.util.regex.Pattern.compile(
            "params\\[(\\d+)\\]\\s*=\\s*\"?([^\"]+)\"?"
        );
        java.util.regex.Matcher matcher = pattern.matcher(value);

        java.util.Set<Integer> foundIndices = new java.util.HashSet<>();

        while (matcher.find()) {
            int index = Integer.parseInt(matcher.group(1));
            String paramName = matcher.group(2).trim();

            JSONObject paramInfo = new JSONObject();
            paramInfo.put("index", index);
            paramInfo.put("name", paramName);
            paramInfo.put("type", "String");
            paramInfo.put("value", paramName);
            paramInfo.put("extracted", true);

            // 배열 크기 확보
            while (parameters.length() <= index) {
                parameters.put(new JSONObject());
            }
            parameters.put(index, paramInfo);
            foundIndices.add(index);
        }

        // 3. 대안 파싱 (params[N] 패턴이 없는 경우)
        if (foundIndices.isEmpty()) {
            String[] parts = value.split("[,\\[\\]]");
            int idx = 0;
            for (String part : parts) {
                part = part.trim();
                if (!part.isEmpty() && !part.contains("params")) {
                    JSONObject paramInfo = new JSONObject();
                    paramInfo.put("index", idx);
                    paramInfo.put("name", "param" + idx);
                    paramInfo.put("type", "Object");
                    paramInfo.put("value", part);
                    parameters.put(paramInfo);
                    idx++;
                }
            }
        }

    } catch (Exception e) {
        System.err.println("[ArthasManager] Failed to parse watch value: " + e.getMessage());
    }

    return parameters;
}
```

**파싱 예시:**
```
입력: params[0]="userId", params[1]="sessionId"
출력: [
  {index: 0, name: "userId", type: "String", value: "userId"},
  {index: 1, name: "sessionId", type: "String", value: "sessionId"}
]
```

---

### 5. 비동기 Watch

#### 5.1 비동기 Watch 시작

```java
public static String startWatchAsync(String className, String methodName, int limit) {
    // 1. Job ID 생성
    String jobId = "watch-" + System.currentTimeMillis();
    String pid = getFirstAvailablePid();

    if (pid.startsWith("ERROR") || pid.isEmpty()) {
        watchStatus.put(jobId, "FAILED");
        watchResults.put(jobId, new JSONObject().put("error", "No running JVM found"));
        return jobId;
    }

    // 2. 상태 설정
    watchStatus.put(jobId, "RUNNING");

    // 3. 백그라운드 스레드에서 실행
    watchExecutor.submit(() -> {
        try {
            String watchCmd = String.format("watch %s %s params -n %d -x 1",
                className, methodName, limit);

            System.out.println("[ArthasManager] Async watch started: " + jobId + " - " + watchCmd);

            // 4. HTTP API 호출
            String result = executeTunnelApiCommand(pid, watchCmd);
            JSONObject response = new JSONObject(result);

            // 5. 파라미터 파싱
            JSONArray parameters = new JSONArray();
            if (response.has("body")) {
                JSONObject body = response.getJSONObject("body");
                if (body.has("results")) {
                    JSONArray results = body.getJSONArray("results");
                    for (int i = 0; i < results.length(); i++) {
                        JSONObject resultItem = results.getJSONObject(i);
                        if ("watch".equals(resultItem.optString("type"))) {
                            String value = resultItem.optString("value", "");
                            parameters = parseWatchValue(value);
                            break;
                        }
                    }
                }
            }

            // 6. 결과 저장
            JSONObject resultJson = new JSONObject();
            resultJson.put("success", true);
            resultJson.put("className", className);
            resultJson.put("methodName", methodName);
            resultJson.put("parameters", parameters);
            resultJson.put("parameterCount", parameters.length());

            watchResults.put(jobId, resultJson);
            watchStatus.put(jobId, "COMPLETED");

            System.out.println("[ArthasManager] Async watch completed: " + jobId);

        } catch (Exception e) {
            System.err.println("[ArthasManager] Async watch failed: " + jobId);
            watchResults.put(jobId, new JSONObject().put("error", e.getMessage()));
            watchStatus.put(jobId, "FAILED");
        }
    });

    return jobId;
}
```

#### 5.2 Watch 결과 조회

```java
public static JSONObject getWatchResult(String jobId) {
    JSONObject result = new JSONObject();
    result.put("jobId", jobId);

    String status = watchStatus.getOrDefault(jobId, "UNKNOWN");
    result.put("status", status);

    if ("COMPLETED".equals(status) || "FAILED".equals(status)) {
        JSONObject data = watchResults.get(jobId);
        if (data != null) {
            // 모든 필드 복사
            for (String key : data.keySet()) {
                result.put(key, data.get(key));
            }
        }
    }

    return result;
}
```

---

### 6. Trace, Stack, Watch 명령

#### 6.1 Trace 명령

```java
public static String trace(String className, String methodName) {
    try {
        String pid = getFirstAvailablePid();
        if (pid.startsWith("ERROR") || pid.isEmpty()) {
            return "ERROR: No running JVM found";
        }

        // trace 명령 생성
        String traceCmd = "trace " + className + " " + methodName + " -n 5";

        // HTTP API로 실행
        return executeTunnelApiCommand(pid, traceCmd);
    } catch (Exception e) {
        return "ERROR: " + e.getMessage();
    }
}
```

#### 6.2 Stack 명령

```java
public static String stack(String className, String methodName) {
    try {
        String pid = getFirstAvailablePid();
        if (pid.startsWith("ERROR") || pid.isEmpty()) {
            return "ERROR: No running JVM found";
        }

        // stack 명령 생성
        String stackCmd = "stack " + className + " " + methodName + " -n 5";

        // HTTP API로 실행
        return executeTunnelApiCommand(pid, stackCmd);
    } catch (Exception e) {
        return "ERROR: " + e.getMessage();
    }
}
```

---

### 7. 클래스 및 메서드 검색

#### 7.1 클래스 검색 (sc 명령)

```java
public static String searchClass(String classNamePattern) {
    try {
        String pid = getFirstAvailablePid();
        if (pid.startsWith("ERROR") || pid.isEmpty()) {
            return "ERROR: No running JVM found";
        }

        // 필요한 경우 부착
        if (!activeSessions.containsKey(pid)) {
            attachByPid(pid);
        }

        // 와일드카드 추가
        String pattern = classNamePattern;
        if (!pattern.contains("*")) {
            pattern = "*" + pattern + "*";
        }

        // sc 명령 실행
        return executeTunnelApiCommand(pid, "sc " + pattern);
    } catch (Exception e) {
        return "ERROR: " + e.getMessage();
    }
}
```

#### 7.2 메서드 검색 (sm 명령)

```java
public static String searchMethod(String className, String methodPattern) {
    try {
        String pid = getFirstAvailablePid();
        if (pid.startsWith("ERROR") || pid.isEmpty()) {
            return "ERROR: No running JVM found";
        }

        // 필요한 경우 부착
        if (!activeSessions.containsKey(pid)) {
            attachByPid(pid);
        }

        // 와일드카드 추가
        String pattern = methodPattern;
        if (!pattern.contains("*")) {
            pattern = "*" + pattern + "*";
        }

        // sm 명령 실행
        return executeTunnelApiCommand(pid, "sm " + className + " " + pattern);
    } catch (Exception e) {
        return "ERROR: " + e.getMessage();
    }
}
```

---

### 8. 프로세스 관리

#### 8.1 PID로 클래스 이름 찾기

```java
public static String findPidByClassName(String className) {
    try {
        String jpsOutput = getJavaProcessList();
        String[] lines = jpsOutput.split("\n");

        for (String line : lines) {
            String[] parts = line.trim().split("\\s+", 2);
            if (parts.length >= 2) {
                String processName = parts[1];

                // 부분 일치 확인
                if (processName.contains(className) ||
                    processName.contains(className.replace(".", "/")) ||
                    processName.toLowerCase().contains(className.toLowerCase())) {
                    return parts[0];
                }
            }
        }
        return "ERROR: No JVM found with class name: " + className;
    } catch (Exception e) {
        return "ERROR: " + e.getMessage();
    }
}
```

#### 8.2 첫 번째 사용 가능한 PID 찾기

```java
private static String getFirstAvailablePid() {
    try {
        String jpsOutput = getJavaProcessList();

        // 파싱
        java.util.List<java.util.Map.Entry<String, String>> pidList = new java.util.ArrayList<>();

        for (String line : jpsOutput.trim().split("\n")) {
            line = line.trim();
            if (line.isEmpty() || line.startsWith("ERROR:")) continue;

            int firstSpace = line.indexOf(' ');
            if (firstSpace > 0) {
                String pid = line.substring(0, firstSpace).trim();
                String className = line.substring(firstSpace + 1).trim();

                // 시스템 프로세스 제외
                if (className.contains("jps") ||
                    className.contains("arthas-boot") ||
                    className.contains("GradleDaemon")) {
                    continue;
                }

                pidList.add(new java.util.AbstractMap.SimpleEntry<>(pid, className));
            }
        }

        // 우선순위 1: server1
        for (java.util.Map.Entry<String, String> entry : pidList) {
            if (entry.getValue().contains("server1")) {
                return entry.getKey();
            }
        }

        // 우선순위 2: agent-server 제외
        for (java.util.Map.Entry<String, String> entry : pidList) {
            if (!entry.getValue().contains("agent-server")) {
                return entry.getKey();
            }
        }

        return "ERROR: No suitable JVM found";
    } catch (Exception e) {
        return "ERROR: " + e.getMessage();
    }
}
```

#### 8.3 Java 프로세스 목록 가져오기

```java
private static String getJavaProcessList() {
    try {
        // 1. jps 시도
        String result = runCommand(new String[]{"jps", "-l"});
        if (!result.isEmpty() && !result.contains("not found")) {
            return result;
        }
    } catch (Exception e) {
        System.out.println("[ArthasManager] jps not available, trying alternative");
    }

    // 2. OS별 대안
    String os = System.getProperty("os.name").toLowerCase();
    if (os.contains("win")) {
        return getJavaProcessesWindows();
    } else {
        return getJavaProcessesUnix();
    }
}
```

**Windows (tasklist):**
```java
private static String getJavaProcessesWindows() {
    ProcessBuilder pb = new ProcessBuilder("cmd.exe", "/c",
        "tasklist /FI \"IMAGENAME eq java.exe\" /FO CSV /NH");

    // CSV 파싱: "java.exe","6036","Console","1","180,000 K"
    // PID 추출 후 WMIC로 명령줄 가져오기
}
```

**Unix (ps):**
```java
private static String getJavaProcessesUnix() {
    ProcessBuilder pb = new ProcessBuilder("bash", "-c",
        "ps -eo pid,command | grep java | grep -v grep");
}
```

---

### 9. 세션 관리

#### 9.1 세션 분리

```java
public static String detach(String pid) {
    ArthasSession session = activeSessions.remove(pid);
    if (session != null) {
        session.stop();
        pidToSessionId.remove(pid);
        return "SUCCESS: Detached from PID " + pid;
    }
    return "ERROR: No session found for PID " + pid;
}
```

#### 9.2 모든 세션 리셋

```java
public static String resetAllSessions() {
    int detachedCount = 0;
    StringBuilder result = new StringBuilder();

    result.append("[ArthasManager] Resetting all sessions...\n");

    for (Map.Entry<String, ArthasSession> entry : activeSessions.entrySet()) {
        String pid = entry.getKey();
        ArthasSession session = entry.getValue();

        try {
            session.stop();
            detachedCount++;
            result.append("  ✓ Detached from PID: ").append(pid).append("\n");
        } catch (Exception e) {
            result.append("  ✗ Failed to detach from PID: ").append(pid)
                  .append(" - ").append(e.getMessage()).append("\n");
        }
    }

    activeSessions.clear();
    pidToSessionId.clear();

    result.append("\n[ArthasManager] Reset complete: ").append(detachedCount)
          .append(" session(s) detached\n");

    return result.toString();
}
```

#### 9.3 ArthasSession 내부 클래스

```java
public static class ArthasSession {
    private final String sessionId;
    private final String pid;
    private final long startTime;
    private final List<String> commandHistory;

    public ArthasSession(String sessionId, String pid) {
        this.sessionId = sessionId;
        this.pid = pid;
        this.startTime = System.currentTimeMillis();
        this.commandHistory = new ArrayList<>();
    }

    public void start() {
        System.out.println("[Arthas] Session started: " + sessionId);
    }

    public void stop() {
        System.out.println("[Arthas] Session stopped: " + sessionId);
    }

    public String executeCommand(String command) {
        commandHistory.add(command);
        System.out.println("[Arthas] Executing: " + command);

        try {
            return executeTunnelApiCommand(this.pid, command);
        } catch (Exception e) {
            return "ERROR executing command: " + e.getMessage();
        }
    }

    // Getters
    public String getSessionId() { return sessionId; }
    public String getPid() { return pid; }
    public long getStartTime() { return startTime; }
    public List<String> getCommandHistory() { return new ArrayList<>(commandHistory); }
}
```

---

## ArthasHttpClient 상세 분석

### 클래스 개요

```java
public class ArthasHttpClient {
    private static final int DEFAULT_ARTHAS_HTTP_PORT = 8561;
    private String host;
    private int port;
}
```

---

### 주요 메서드

#### 1. 명령 실행

```java
public String executeCommand(String command) throws IOException {
    String urlStr = String.format("http://%s:%d/api", host, port);
    URL url = new URL(urlStr);
    HttpURLConnection conn = (HttpURLConnection) url.openConnection();

    try {
        conn.setRequestMethod("POST");
        conn.setRequestProperty("Content-Type", "application/json");
        conn.setDoOutput(true);

        // JSON 요청 본문
        String jsonBody = String.format(
            "{\"action\":\"exec\",\"command\":\"%s\"}",
            command.replace("\"", "\\\"")
        );

        try (OutputStream os = conn.getOutputStream()) {
            byte[] input = jsonBody.getBytes(StandardCharsets.UTF_8);
            os.write(input, 0, input.length);
        }

        int responseCode = conn.getResponseCode();
        if (responseCode == HttpURLConnection.HTTP_OK) {
            return readResponse(conn.getInputStream());
        } else {
            return "ERROR: HTTP " + responseCode + " - " + readResponse(conn.getErrorStream());
        }
    } finally {
        conn.disconnect();
    }
}
```

#### 2. 상태 조회

```java
public String getStatus() throws IOException {
    String urlStr = String.format("http://%s:%d/api", host, port);
    URL url = new URL(urlStr);
    HttpURLConnection conn = (HttpURLConnection) url.openConnection();

    try {
        conn.setRequestMethod("GET");
        conn.setRequestProperty("Content-Type", "application/json");

        int responseCode = conn.getResponseCode();
        if (responseCode == HttpURLConnection.HTTP_OK) {
            return readResponse(conn.getInputStream());
        } else {
            return "ERROR: HTTP " + responseCode;
        }
    } finally {
        conn.disconnect();
    }
}
```

#### 3. 응답 읽기

```java
private String readResponse(InputStream stream) throws IOException {
    if (stream == null) {
        return "";
    }

    StringBuilder response = new StringBuilder();
    try (BufferedReader reader = new BufferedReader(
            new InputStreamReader(stream, StandardCharsets.UTF_8))) {
        String line;
        while ((line = reader.readLine()) != null) {
            response.append(line);
        }
    }
    return response.toString();
}
```

---

## HTTP API 명세

### Arthas HTTP API (포트 8563)

#### 1. 명령 실행

```http
POST /api
Content-Type: application/json

{
  "action": "exec",
  "command": "watch com.example.MyClass myMethod '{params}' -n 1",
  "execTimeout": "60000"
}
```

**응답:**
```json
{
  "state": "SUCCEEDED",
  "body": {
    "results": [
      {
        "type": "watch",
        "value": "params[0]=\\"userId\\", params[1]=\\"12345\\""
      }
    ]
  }
}
```

#### 2. 지원 명령

| 명령 | 설명 | 예시 |
|------|------|------|
| `watch` | 메서드 호출 감시 | `watch com.example.MyClass myMethod '{params}' -n 5` |
| `trace` | 메서드 호출 추적 | `trace com.example.MyClass myMethod -n 5` |
| `stack` | 스택 트레이스 | `stack com.example.MyClass myMethod -n 5` |
| `sc` | 클래스 검색 | `sc *MyClass*` |
| `sm` | 메서드 검색 | `sm com.example.MyClass get*` |
| `classloader` | 클래스로더 정보 | `classloader -t` |
| `thread` | 스레드 정보 | `thread` |

---

## 사용 예시

### 1. Arthas 부착 및 Watch

```java
// 1. 부착
String result = ArthasManager.attachByPid("12345");
// 결과: SUCCESS: Arthas attached to PID 12345, HTTP API on port 8563

// 2. Watch 실행
JSONObject watchResult = ArthasManager.watchWithParameters(
    "com.test.service.UserIdService",
    "setUserId",
    5
);

// 3. 파라미터 확인
JSONArray parameters = watchResult.getJSONArray("parameters");
for (int i = 0; i < parameters.length(); i++) {
    JSONObject param = parameters.getJSONObject(i);
    System.out.println("param[" + param.getInt("index") + "] = " + param.getString("name"));
}
```

### 2. 비동기 Watch

```java
// 1. 비동기 watch 시작
String jobId = ArthasManager.startWatchAsync(
    "com.test.service.UserIdService",
    "setUserId",
    1
);

// 2. 사용자가 메서드 호출
// ...

// 3. 결과 확인
JSONObject result = ArthasManager.getWatchResult(jobId);
String status = result.getString("status"); // "RUNNING", "COMPLETED", "FAILED"

if ("COMPLETED".equals(status)) {
    JSONArray parameters = result.getJSONArray("parameters");
    // 파라미터 처리
}
```

### 3. Trace 및 Stack

```java
// Trace
String traceResult = ArthasManager.trace(
    "com.example.MyClass",
    "myMethod"
);

// Stack
String stackResult = ArthasManager.stack(
    "com.example.MyClass",
    "myMethod"
);
```

### 4. 클래스 및 메서드 검색

```java
// 클래스 검색
String classes = ArthasManager.searchClass("MyClass");
// 결과: com.example.MyClass
//       com.test.MyClass

// 메서드 검색
String methods = ArthasManager.searchMethod("com.example.MyClass", "get*");
// 결과: public java.lang.String com.example.MyClass.getName()
//       public java.lang.Integer com.example.MyClass.getAge()
```

---

## 트러블슈팅

### 1. Arthas 부착 실패

**증상:** `ERROR: Failed to attach Arthas`

**원인:**
- PID가 존재하지 않음
- Arthas boot jar가 없음
- 포트가 이미 사용 중

**해결:**
```bash
# 1. PID 확인
jps -l

# 2. Arthas 다운로드
curl -O https://arthas.aliyun.com/arthas-boot.jar

# 3. 포트 확인
netstat -an | grep 8563
```

---

### 2. Watch 결과 없음

**증상:** `Extracted 0 parameters`

**원인:**
- 메서드가 호출되지 않음
- limit 설정이 너무 낮음
- 파라미터가 없는 메서드

**해결:**
```bash
# 1. 메서드 호출 여부 확인
# Arthas 콘솔에서 직접 watch 실행

# 2. limit 증가
JSONObject result = ArthasManager.watchWithParameters(className, methodName, 100);

# 3. 메서드 시그니처 확인
String methods = ArthasManager.searchMethod(className, methodName);
```

---

### 3. HTTP API 연결 실패

**증상:** `API call failed: Connection refused`

**원인:**
- Arthas HTTP API가 시작되지 않음
- 포트 불일치
- 방화벽

**해결:**
```bash
# 1. Arthas 프로세스 확인
ps aux | grep arthas

# 2. 포트 확인
curl http://127.0.0.1:8563/api

# 3. 방화벽 확인
sudo ufw allow 8563
```

---

### 4. jps not found

**증상:** `ERROR: No suitable JVM found`

**원인:**
- JAVA_HOME이 설정되지 않음
- jps가 PATH에 없음

**해결:**
```bash
# 1. JAVA_HOME 확인
echo $JAVA_HOME

# 2. jps 경로 확인
which jps

# 3. Windows에서는 tasklist 사용
tasklist | findstr java.exe
```

---

## 요약

### ArthasManager 핵심 기능

| 기능 | 메서드 | 설명 |
|------|--------|------|
| 부착 | `attachByPid()`, `attachByClassName()` | Arthas를 JVM에 부착 |
| 분리 | `detach()`, `resetAllSessions()` | Arthas 세션 종료 |
| Watch | `watchWithParameters()` | 파라미터 추출 |
| 비동기 | `startWatchAsync()`, `getWatchResult()` | 비동기 watch |
| 추적 | `trace()`, `stack()` | 실행 흐름 추적 |
| 검색 | `searchClass()`, `searchMethod()` | 클래스/메서드 검색 |
| 프로세스 | `findPidByClassName()`, `listJavaProcesses()` | 프로세스 관리 |

### 데이터 플로우

```
┌─────────────┐
│   REST API  │
└──────┬──────┘
       │
       ↓
┌─────────────────────────────────────────────────┐
│           ArthasManager                         │
│  ┌──────────────────────────────────────────┐  │
│  │  RestTemplate (HTTP 통신)                │  │
│  └──────────────┬───────────────────────────┘  │
└─────────────────┼──────────────────────────────┘
                  │
                  ↓
        ┌─────────────────┐
        │ Arthas HTTP API │
        │   :8563/api     │
        └────────┬─────────┘
                 │
                 ↓
        ┌─────────────────┐
        │  arthas-boot    │
        │  (Target JVM)   │
        └─────────────────┘
```

---

**분석 완료일:** 2026-04-03
**분석자:** Claude Code
