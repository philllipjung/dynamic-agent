# 스레드명 변경 기능 구현 완료 보고서

## 📋 개요
**날짜**: 2026-03-27
**최종 수정일**: 2026-05-04
**기능**: 메서드 실행 중 스레드명을 TraceId/SpanId 기반으로 변경
**상태**: ✅ 구현 완료 및 테스트 성공
**작업 시간**: 약 5시간 (문제 해결 포함)

## 🎯 목표

메서드가 실행될 때 해당 요청의 TraceId와 SpanId를 포함한 고유한 스레드명으로 변경하여 eBPF/커널 레벨 트레이싱에서 추적 가능하게 만듦

**스레드명 형식 (최종):**
```
{TraceId 6자}{SpanId 6자}{이니셜 1자}{숫자 2자}
예: a7505712f3f1H01
   ------^^^^^^^^-^----
   Trace  Span   INu
   6자    6자    이 2
                니
                자
                숫
                                        자
```

**이니셜 규칙:** 원래 스레드명의 첫 번째 알파벳
- `http-nio-8081-exec-1` → **H** (http)
- `reactor-http-nio-10` → **R** (reactor)
- `pool-1-thread-3` → **P** (pool)

**숫자 규칙:** 원래 스레드명의 마지막 숫자 (2자리)
- `http-nio-8081-exec-1` → **01**
- `reactor-http-nio-10` → **10**
- `pool-1-thread-3` → **03**

---

## 📝 소스 변경사항

### 1. ThreadNameHelper 클래스 생성 (신규)

**파일**: `bytebuddy-advice/src/main/java/com/javaagent/bytebuddy/helper/ThreadNameHelper.java`

**전체 코드:**
```java
package com.javaagent.bytebuddy.helper;

import io.opentelemetry.api.trace.Span;
import io.opentelemetry.api.trace.SpanContext;
import io.opentelemetry.context.Context;

import java.util.concurrent.ThreadLocalRandom;
import java.util.regex.Pattern;

/**
 * 스레드명 관리 Helper
 *
 * OpenTelemetry TraceId와 SpanId를 사용하여 고유한 스레드명 생성
 */
public class ThreadNameHelper {

    // 스레드명을 변경할 대상 패턴
    private static final Pattern[] TARGET_PATTERNS = {
        Pattern.compile(".*")  // 🔴 TEMP: 모든 스레드에 적용 (테스트용)
    };

    // 이니셜 (1자리, 무작위 선택용)
    private static final String[] INITIALS = {
        "S", "A", "W", "D", "T", "P", "R", "H", "X", "Z"
    };

    /**
     * 현재 스레드의 이름이 변경 대상인지 확인
     */
    public static boolean shouldRenameThread() {
        String currentName = Thread.currentThread().getName();
        for (Pattern pattern : TARGET_PATTERNS) {
            if (pattern.matcher(currentName).matches()) {
                return true;
            }
        }
        return false;
    }

    /**
     * 현재 스레드의 이름을 변경 (실제 추적 컨텍스트에서 TraceId/SpanId 읽기)
     *
     * 형식: {TraceId 7자}{SpanId 6자}{이니셜}{숫자}
     * 예: 26cd7480d92c2DBG6176
     */
    public static void renameThread() {
        renameThread(Span.current());
    }

    /**
     * 현재 스레드의 이름을 변경 (Span에서 SpanContext 직접 읽기)
     *
     * @param span Span 객체 (null 가능)
     */
    public static void renameThread(Span span) {
        String currentName = Thread.currentThread().getName();

        // Span에서 SpanContext 가져오기
        if (span == null || !span.getSpanContext().isValid()) {
            return;
        }

        SpanContext context = span.getSpanContext();
        String traceId = context.getTraceId();
        String spanId = context.getSpanId();

        if (traceId == null || spanId == null) {
            return;
        }

        // 포맷: {TraceId 6자}{SpanId 6자}{이니셜 1자}{숫자 2자}
        String newThreadName = formatThreadName(traceId, spanId, currentName);

        // 스레드명 변경
        try {
            Thread.currentThread().setName(newThreadName);
            // 로그에 TraceId와 SpanId 출력
            System.out.println("===============================================");
            System.out.println("[ThreadName] RENAMED THREAD");
            System.out.println("[ThreadName] Original Name: " + currentName);
            System.out.println("[ThreadName] New Name:      " + newThreadName);
            System.out.println("[ThreadName] TraceId:       " + traceId);
            System.out.println("[ThreadName] SpanId:        " + spanId);
            System.out.println("===============================================");
        } catch (Exception e) {
            System.err.println("[ThreadName] Failed to rename thread: " + e.getMessage());
        }
    }

    /**
     * TraceId와 SpanId로 형식화된 스레드명 생성
     * 형식: {TraceId 6자}{SpanId 6자}{이니셜 1자}{숫자 2자}
     * 예: a7505712f3f1H01 (http-nio-8081-exec-1)
     *     cf1f5a5aa57bR10 (reactor-http-nio-10)
     */
    private static String formatThreadName(String traceId, String spanId, String currentName) {
        // TraceId 6자 (앞 6자)
        String tracePrefix = traceId.substring(0, Math.min(6, traceId.length()));

        // SpanId 6자 (앞 6자)
        String spanPrefix = spanId.substring(0, Math.min(6, spanId.length()));

        // 이니셜 1자 (원래 스레드명의 첫 번째 알파벳)
        String initial = extractInitial(currentName);

        // 숫자 2자 (원래 스레드명에서 추출, 없으면 무작위)
        int threadNumber = extractThreadNumber(currentName);

        return String.format("%s%s%s%02d", tracePrefix, spanPrefix, initial, threadNumber);
    }

    /**
     * 스레드명에서 첫 번째 알파벳(이니셜) 추출
     * 예: http-nio-8081-exec-2 → H
     *     reactor-http-nio-10 → R
     *     pool-1-thread-3 → P
     */
    private static String extractInitial(String threadName) {
        if (threadName == null || threadName.isEmpty()) {
            return INITIALS[Math.abs(ThreadLocalRandom.current().nextInt()) % INITIALS.length];
        }

        // 첫 번째 단어의 첫 번째 알파벳 찾기
        for (int i = 0; i < threadName.length(); i++) {
            char c = threadName.charAt(i);
            if (Character.isLetter(c)) {
                return String.valueOf(Character.toUpperCase(c));
            }
        }

        // 알파벳을 찾지 못하면 무작위 이니셜 반환
        return INITIALS[Math.abs(ThreadLocalRandom.current().nextInt()) % INITIALS.length];
    }

    /**
     * 스레드명에서 숫자 추출
     * 예: reactor-http-nio-10 → 10
     *     http-nio-8081-exec-2 → 2
     *     pool-1-thread-3 → 3
     */
    private static int extractThreadNumber(String threadName) {
        if (threadName == null) {
            return ThreadLocalRandom.current().nextInt(100);
        }

        // 스레드명의 마지막 숫자 추출
        java.util.regex.Pattern numberPattern = java.util.regex.Pattern.compile("(\\d+)$");
        java.util.regex.Matcher matcher = numberPattern.matcher(threadName);

        if (matcher.find()) {
            try {
                int number = Integer.parseInt(matcher.group(1));
                // 2자리로 제한 (0-99)
                return number % 100;
            } catch (NumberFormatException e) {
                // 숫자 변환 실패 시 무작위 값 반환
            }
        }

        // 숫자를 찾지 못하면 무작위 값 반환
        return ThreadLocalRandom.current().nextInt(100);
    }

    /**
     * 현재 스레드명이 변경 대상 패턴인지 확인
     */
    public static boolean isTargetThread() {
        return shouldRenameThread();
    }
}
```

**주요 변경사항:**
- ✅ SpanContext에서 TraceId/SpanId 추출
- ✅ 형식화된 스레드명 생성 (7자 + 6자 + 3자 + 4자)
- ✅ Thread.currentThread().setName() 호출
- ✅ 대상 스레드 패턴 매칭 (현재는 모든 스레드 대상)

### 2. SpanAdvice 수정 (스레드명 변경 로직 추가)

**파일**: `bytebuddy-advice/src/main/java/com/javaagent/bytebuddy/advices/SpanAdvice.java`

**변경 1: inline=false로 변경**
```java
// 변경 전
@Advice.OnMethodEnter(inline = true)
@Advice.OnMethodExit(inline = true)

// 변경 후
@Advice.OnMethodEnter(inline = false)
@Advice.OnMethodExit(inline = false)
```

**변경 2: OnMethodEnter에 스레드명 변경 로직 추가**
```java
@Advice.OnMethodEnter(inline = false)
public static SpanHelper onMethodEnter(
        @Advice.Origin String method,
        @Advice.This Object target,
        @Advice.AllArguments Object[] allArguments
) {
    // 🆕 스레드명 변경: 원래 이름 저장
    String originalThreadName = Thread.currentThread().getName();
    boolean renamedThread = false;

    System.err.println(">>> ADVICE ENTER: " + method);

    // ... 기존 코드 (Span 생성, 파라미터 추출 등) ...

    // SpanHelper를 사용하여 span 생성 (Links 포함)
    SpanHelper spanHelper = SpanHelper.createSpanWithLinks(method, linkedContexts);
    System.err.println(">>> SPAN CREATED: " + (spanHelper != null && spanHelper.isValid()));

    // ===== 🆕 스레드명 변경 =====
    System.err.println(">>> [ThreadName] Check: spanHelper=" + (spanHelper != null && spanHelper.isValid()) + ", isTarget=" + ThreadNameHelper.isTargetThread());
    System.err.println(">>> [ThreadName] Current thread: " + Thread.currentThread().getName());
    if (spanHelper != null && spanHelper.isValid() && ThreadNameHelper.isTargetThread()) {
        // 실제 추적 컨텍스트의 Span 객체 전달
        System.err.println(">>> [ThreadName] Renaming thread...");
        ThreadNameHelper.renameThread(spanHelper.getSpan());
        renamedThread = true;
        // 원래 스레드명을 spanHelper에 저장하여 나중에 복원
        spanHelper.setOriginalThreadName(originalThreadName);
    }
    // =============================

    // ... 파라미터 속성 추가 코드 ...

    return spanHelper;
}
```

**변경 3: OnMethodExit에 스레드명 복원 로직 추가**
```java
@Advice.OnMethodExit(inline = false)
public static void onMethodExit(@Advice.Enter SpanHelper spanHelper) {
    System.err.println("<<< ADVICE EXIT");

    if (spanHelper != null && spanHelper.isValid()) {
        spanHelper.complete();
        System.err.println("<<< SPAN COMPLETED");

        // ===== 🆕 스레드명 복원 =====
        String originalThreadName = spanHelper.getOriginalThreadName();
        if (originalThreadName != null) {
            try {
                String currentName = Thread.currentThread().getName();
                Thread.currentThread().setName(originalThreadName);
                System.out.println("===============================================");
                System.out.println("[ThreadName] RESTORED THREAD");
                System.out.println("[ThreadName] From: " + currentName);
                System.out.println("[ThreadName] To:   " + originalThreadName);
                System.out.println("===============================================");
            } catch (Exception e) {
                System.err.println("<<< Failed to restore thread name: " + e.getMessage());
            }
        }
        // =============================
    }
}
```

### 3. SpanHelper 수정 (originalThreadName 필드 추가)

**파일**: `bytebuddy-advice/src/main/java/com/javaagent/bytebuddy/helper/SpanHelper.java`

**변경사항:**
```java
public class SpanHelper {
    private Span span;
    private boolean spanCompleted = false;
    private String originalThreadName;  // 🆕 원래 스레드명 저장

    // 🆕 원래 스레드명 setter/getter
    public void setOriginalThreadName(String name) {
        this.originalThreadName = name;
    }

    public String getOriginalThreadName() {
        return originalThreadName;
    }

    // ... 기존 코드 ...
}
```

### 4. AgentConstants 수정 (ThreadNameHelper 추가)

**파일**: `agent-commons/src/main/java/com/javaagent/commons/AgentConstants.java`

**변경사항:**
```java
// 변경 전
public static final String[] HELPER_CLASSES = {
    "com.javaagent.bytebuddy.advances.SpanAdvice",  // ❌ 잘못됨
    "com.javaagent.bytebuddy.helper.SpanAttributeHelper",
    "com.javaagent.bytebuddy.helper.SpanHelper",
    "com.javaagent.bytebuddy.helper.JaegerLinkLookupService",
    "com.javaagent.bytebuddy.advices.KernelAdvice",
    "com.javaagent.bytebuddy.helper.KernelHelper"
};

// 변경 후
public static final String[] HELPER_CLASSES = {
    // SpanAdvice 제거됨 (이미 agent JAR에 있음)
    "com.javaagent.bytebuddy.helper.SpanAttributeHelper",
    "com.javaagent.bytebuddy.helper.SpanHelper",
    "com.javaagent.bytebuddy.helper.JaegerLinkLookupService",
    "com.javaagent.bytebuddy.helper.ThreadNameHelper",  // ✅ 추가됨
    "com.javaagent.bytebuddy.advices.KernelAdvice",
    "com.javaagent.bytebuddy.helper.KernelHelper"
};
```

### 5. ByteBuddyAgent 수정 (이미 로드된 클래스 체크)

**파일**: `bytebuddy-agent/src/main/java/com/javaagent/bytebuddy/ByteBuddyAgent.java`

**변경사항:**
```java
private static boolean injectHelper(ClassLoader targetLoader) {
    if (targetLoader == null) {
        System.err.println("[ByteBuddy] Target ClassLoader is null, cannot inject helper");
        return false;
    }

    try {
        for (String helperClassName : AgentConstants.HELPER_CLASSES) {
            // 🆕 이미 로드된 클래스인지 체크
            try {
                targetLoader.loadClass(helperClassName);
                System.out.println("[ByteBuddy] Helper already loaded: " + helperClassName + ", skipping injection");
                continue;
            } catch (ClassNotFoundException e) {
                // Class not loaded, proceed with injection
            }

            // Load helper class bytecode from System ClassLoader
            byte[] classBytes = loadHelperClassBytes(helperClassName);
            if (classBytes == null) {
                System.err.println("[ByteBuddy] Failed to load bytecode for " + helperClassName);
                continue;
            }

            // Inject into target ClassLoader using reflection
            injectClassIntoLoader(targetLoader, helperClassName, classBytes);
            System.out.println("[ByteBuddy] Injected helper: " + helperClassName + " into ClassLoader: " + targetLoader);
        }

        return true;

    } catch (Exception e) {
        System.err.println("[ByteBuddy] Failed to inject helper classes: " + e.getMessage());
        e.printStackTrace();
        return false;
    }
}
```

---

## 🔥 발생한 문제점

### 문제 1: ThreadName 로그가 전혀 출력되지 않음

**증상:**
```
>>> ADVICE ENTER: public java.lang.String com.test.service.test1.controller.Test1Controller.test1(java.lang.String)
[SpanHelper] Span created and made current: ...
>>> SPAN CREATED: true
>>> Set attribute: userId = THREAD_NAME_FORMAT_TEST_1774616915
<<< ADVICE EXIT

# 예상되었던 로그:
>>> [ThreadName] Check: spanHelper=true, isTarget=true
===============================================
[ThreadName] RENAMED THREAD
...
```

**원인 분석:**
1. Advice 자체는 실행됨 (`>>> ADVICE ENTER`, `>>> SPAN CREATED` 확인)
2. 하지만 ThreadName 관련 로그가 전혀 없음
3. `Transformed bytecode created, size: 6458` - 항상 같은 크기

**결론:** 소스 코드 변경사항이 런타임에 반영되지 않음

### 문제 2: Helper injection 실패 (중복 클래스 정의)

**증상:**
```
[ByteBuddy] Failed to inject helper classes: null
java.lang.reflect.InvocationTargetException
Caused by: java.lang.LinkageError: loader 'app' attempted duplicate class definition for com.javaagent.bytebuddy.advices.SpanAdvice
```

**원인:**
1. SpanAdvice가 이미 agent JAR에서 로드되어 있음
2. HELPER_CLASSES에 SpanAdvice가 포함되어 있어서 다시 injection 시도
3. 중복 정의 에러 발생

### 문제 3: JAR 파일 손상

**증상:**
```
[ByteBuddy] Error loading helper bytecode for com.javaagent.bytebuddy.advices.SpanAdvice: ZipFile invalid LOC header (bad signature)
```

**원인:**
1. `cp` 명령어로 JAR 파일 복사 시 파일이 손상됨
2. Windows 환경에서 파일 사용 중인 경우 복사 실패

### 문제 4: Maven 로컬 리포지토리 미설치

**증상:**
```
# 파일 시간 확인
agent-commons의 AgentConstants.class: 22:40 (최신!)
bytebuddy-agent JAR의 AgentConstants.class: 20:01 (옛날 버전!)
```

**로그 확인:**
```
[ByteBuddy] Helper already loaded: com.javaagent.bytebuddy.advices.SpanAdvice
[ByteBuddy] Helper already loaded: com.javaagent.bytebuddy.helper.SpanAttributeHelper
[ByteBuddy] Helper already loaded: com.javaagent.bytebuddy.helper.SpanHelper
[ByteBuddy] Helper already loaded: com.javaagent.bytebuddy.helper.JaegerLinkLookupService
# ❌ ThreadNameHelper가 목록에 없음!
```

**원인:**
1. `mvn package`만 실행하여 로컬 리포지토리에 설치 안 됨
2. bytebuddy-agent가 Maven 로컬 리포지토리(`~/.m2/repository`)에서 agent-commons를 가져옴
3. 그곳에 있는 게 예전 버전임

### 문제 5: inline=true로 bytecode hard-coded

**증상:**
```
[ByteBuddy] Transformed bytecode created, size: 6458
```
- 항상 같은 크기의 bytecode가 생성됨
- inline=false로 변경했는데도 변화 없음

**원인:**
1. bytebuddy-advice도 `mvn package`만 해서 로컬 리포지토리에 설치 안 됨
2. bytebuddy-agent가 예전 버전의 SpanAdvice를 가져옴
3. inline=true로 설정된 예전 bytecode가 그대로 사용됨

---

## 🔍 트러블슈팅 과정

### 시도 1: 소스 수정 후 빌드 및 재attach

**과정:**
```bash
cd bytebuddy-advice && mvn clean package
cd bytebuddy-agent && mvn clean package
cp target/bytebuddy-agent-1.0.0.jar ../agent-server/

# detach 후 재 attach
curl -X DELETE "http://localhost:8080/api/bytebuddy/detach?pid=..."
curl -X POST http://localhost:8080/api/bytebuddy/attach ...
```

**결과:** ❌ ThreadName 로그 없음

**원인:**
- 소스 코드 수정이 반영되지 않음
- JVM이 이미 transform된 bytecode를 캐시하고 있음

### 시도 2: JVM 완전 재시작

**과정:**
```bash
taskkill //F //IM java.exe
# 서비스 재시작
# agent 재부착
```

**결과:** ❌ 여전히 ThreadName 로그 없음

**원인:**
- JAR 파일 자체에 예전 코드가 들어있음
- 단순 재시작으로는 해결 안 됨

### 시도 3: --add-opens 옵션 추가

**과정:**
```xml
<!-- test1-service/pom.xml -->
<configuration>
    <jvmArguments>
        --add-opens java.base/java.lang=ALL-UNNAMED
        --add-opens java.base/java.lang.reflect=ALL-UNNAMED
        --add-opens java.base/java.io=ALL-UNNAMED
    </jvmArguments>
</configuration>
```

**결과:** ❌ 여전히 ThreadName 로그 없음

**하지만 진전 있음:**
```
# 이전 에러:
Unable to make protected final java.lang.Class java.lang.ClassLoader.defineClass(...)
accessible: module java.base does not "opens java.lang" to unnamed module

# 현재 에러:
java.lang.LinkageError: loader 'app' attempted duplicate class definition for com.javaagent.bytebuddy.advices.SpanAdvice
```

--add-opens 옵션이 작동하여 ClassLoader.defineClass에 접근 가능하게 됨!

### 시도 4: AgentConstants에서 SpanAdvice 제거

**과정:**
```java
// AgentConstants.java
public static final String[] HELPER_CLASSES = {
    // SpanAdvice 제거
    "com.javaagent.bytebuddy.helper.SpanAttributeHelper",
    "com.javaagent.bytebuddy.helper.SpanHelper",
    "com.javaagent.bytebuddy.helper.JaegerLinkLookupService",
    "com.javaagent.bytebuddy.helper.ThreadNameHelper",  // 추가
    ...
};
```

**결과:** ❌ ThreadNameHelper가 여전히 목록에 없음

**원인:** agent-commons 변경사항이 JAR에 반영되지 않음

### 시도 5: JAR 파일 타임스탬프 확인

**과정:**
```bash
# 파일 시간 확인
agent-commons/target/classes/AgentConstants.class: 22:40 (최신!)
bytebuddy-agent/target/bytebuddy-agent-1.0.0.jar/AgentConstants.class: 20:01 (옛날 버전!)
```

**발견:** Maven 로컬 리포지토리 미설치 문제!

### 시도 6: mvn install 사용 (성공!)

**과정:**
```bash
# 1. agent-commons를 로컬 리포지토리에 설치
cd agent-commons && mvn clean install

# 2. bytebuddy-advice를 로컬 리포지토리에 설치
cd bytebuddy-advice && mvn clean install

# 3. bytebuddy-agent 재빌드
cd bytebuddy-agent && mvn clean package

# 4. 타임스탬프 확인
unzip -l target/bytebuddy-agent-1.0.0.jar | grep AgentConstants
# 결과: 2026-03-27 22:55   com/javaagent/commons/AgentConstants.class ✅

# 5. SpanAdvice 확인
unzip -l target/bytebuddy-agent-1.0.0.jar | grep SpanAdvice
# 결과: 2026-03-27 22:57   com/javaagent/bytebuddy/advices/SpanAdvice.class ✅
```

**결과:** ✅ ThreadNameHelper가 목록에 나타남!

```
[ByteBuddy] Helper already loaded: com.javaagent.bytebuddy.helper.ThreadNameHelper, skipping injection
```

### 시도 7: 최종 테스트 (완전 성공!)

**과정:**
```bash
# 1. test1 서비스 재시작
taskkill //F //PID ...
cd test-services/test1-service && mvn spring-boot:run

# 2. agent attach
curl -X POST http://localhost:8080/api/bytebuddy/attach ...

# 3. 테스트 요청
curl http://localhost:8081/test1?userId=TEST_12345
```

**결과:** ✅ 완전 성공!

```
>>> [ThreadName] Check: spanHelper=true, isTarget=true
>>> [ThreadName] Current thread: http-nio-8081-exec-2
>>> [ThreadName] Renaming thread...
===============================================
[ThreadName] RENAMED THREAD
[ThreadName] Original Name: http-nio-8081-exec-2
[ThreadName] New Name:      26cd7480d92c2DBG6176
[ThreadName] TraceId:       26cd7480a4e31649170c5ea32125ea32
[ThreadName] SpanId:        0d92c22d29635a45
===============================================
>>> Set attribute: userId = TEST_12345
<<< ADVICE EXIT
===============================================
[ThreadName] RESTORED THREAD
[ThreadName] From: 26cd7480d92c2DBG6176
[ThreadName] To:   http-nio-8081-exec-2
===============================================
```

---

## ✅ 해결책

### 해결책 1: Maven 로컬 리포지토리 설치 (핵심 해결책)

**문제:**
```bash
# ❌ 잘못된 방법
cd agent-commons && mvn clean package
cd bytebuddy-agent && mvn clean package
# → 다른 모듈에서 예전 버전을 가져옴
```

**해결:**
```bash
# ✅ 올바른 방법
cd agent-commons && mvn clean install
cd bytebuddy-advice && mvn clean install
cd bytebuddy-agent && mvn clean package
# → 로컬 리포지토리에 최신 버전 설치
```

**이유:**
- `mvn package`는 프로젝트를 빌드하지만 로컬 리포지토리(`~/.m2/repository`)에 설치하지 않음
- 다중 모듈 프로젝트에서는 의존성 모듈을 먼저 `install` 해야 함
- 그렇지 않으면 다른 모듈이 로컬 리포지토리에 있는 예전 버전을 가져옴

### 해결책 2: inline=false로 변경 (개발용)

**문제:**
- `inline=true`로 설정되어 있어서 Advice 코드가 bytecode에 직접 복사됨
- 소스 수정이 반영되지 않음

**해결:**
```java
// 변경 전
@Advice.OnMethodEnter(inline = true)
@Advice.OnMethodExit(inline = true)

// 변경 후 (개발용)
@Advice.OnMethodEnter(inline = false)
@Advice.OnMethodExit(inline = false)
```

**이유:**
- `inline=true`: Advice 코드를 target 메서드의 bytecode에 직접 복사
  - 장점: 메서드 호출 오버헤드 없음
  - 단점: 소스 수정 반영 안 됨, 디버깅 어려움

- `inline=false`: Advice 코드를 메서드 호출 방식으로 실행
  - 장점: 소스 수정 즉시 반영, 디버깅 쉬움
  - 단점: 메서드 호출 1회 추가 (성능 차이 미미함)

### 해결책 3: HELPER_CLASSES에서 SpanAdvice 제거

**문제:**
- SpanAdvice가 이미 agent JAR에 포함되어 있는데 다시 injection 시도
- 중복 클래스 정의 에러 발생

**해결:**
```java
// AgentConstants.java
public static final String[] HELPER_CLASSES = {
    // SpanAdvice 제거 (이미 agent JAR에 있음)
    "com.javaagent.bytebuddy.helper.SpanAttributeHelper",
    "com.javaagent.bytebuddy.helper.SpanHelper",
    "com.javaagent.bytebuddy.helper.JaegerLinkLookupService",
    "com.javaagent.bytebuddy.helper.ThreadNameHelper",  // 추가
    "com.javaagent.bytebuddy.advices.KernelAdvice",
    "com.javaagent.bytebuddy.helper.KernelHelper"
};
```

### 해결책 4: 이미 로드된 클래스 체크 로직 추가

**문제:**
- 이미 로드된 클래스를 다시 injection 시도
- 불필요한 작업과 잠재적 오류

**해결:**
```java
// ByteBuddyAgent.java
for (String helperClassName : AgentConstants.HELPER_CLASSES) {
    // 이미 로드된 클래스인지 체크
    try {
        targetLoader.loadClass(helperClassName);
        System.out.println("[ByteBuddy] Helper already loaded: " + helperClassName + ", skipping injection");
        continue;
    } catch (ClassNotFoundException e) {
        // Class not loaded, proceed with injection
    }

    // ... injection 로직 ...
}
```

---

## 📊 최종 테스트 결과

### 테스트 환경
- OS: Windows 11
- Java: 21.0.9
- Spring Boot: 3.2.5
- ByteBuddy: 1.x
- OpenTelemetry: 1.38.0

### 테스트 1: 첫 번째 요청

**요청:**
```bash
curl http://localhost:8081/test1?userId=THREAD_NAME_HELPER_TEST_22222
```

**결과:**
```
>>> [ThreadName] Check: spanHelper=true, isTarget=true
>>> [ThreadName] Current thread: http-nio-8081-exec-2
>>> [ThreadName] Renaming thread...
===============================================
[ThreadName] RENAMED THREAD
[ThreadName] Original Name: http-nio-8081-exec-2
[ThreadName] New Name:      26cd7480d92c2DBG6176
[ThreadName] TraceId:       26cd7480a4e31649170c5ea32125ea32
[ThreadName] SpanId:        0d92c22d29635a45
===============================================
>>> Set attribute: userId = THREAD_NAME_HELPER_TEST_22222
<<< ADVICE EXIT
===============================================
[ThreadName] RESTORED THREAD
[ThreadName] From: 26cd7480d92c2DBG6176
[ThreadName] To:   http-nio-8081-exec-2
===============================================
```

**분석:**
- ✅ 스레드명 변경: `http-nio-8081-exec-2` → `26cd7480d92c2DBG6176`
- ✅ TraceId 추출: `26cd7480a4e31649170c5ea32125ea32`
- ✅ SpanId 추출: `0d92c22d29635a45`
- ✅ 스레드명 복원: `26cd7480d92c2DBG6176` → `http-nio-8081-exec-2`

### 테스트 2: 두 번째 요청

**요청:**
```bash
curl http://localhost:8081/test1?userId=SECOND_TEST_99999
```

**결과:**
```
>>> [ThreadName] Check: spanHelper=true, isTarget=true
>>> [ThreadName] Current thread: http-nio-8081-exec-3
>>> [ThreadName] Renaming thread...
===============================================
[ThreadName] RENAMED THREAD
[ThreadName] Original Name: http-nio-8081-exec-3
[ThreadName] New Name:      81f20cb2607a4DEV1441
[ThreadName] TraceId:       81f20cbf9408626b49db829f0a40b106
[ThreadName] SpanId:        2607a4cd2b22cab7
===============================================
>>> Set attribute: userId = SECOND_TEST_99999
<<< ADVICE EXIT
===============================================
[ThreadName] RESTORED THREAD
[ThreadName] From: 81f20cb2607a4DEV1441
[ThreadName] To:   http-nio-8081-exec-3
===============================================
```

**분석:**
- ✅ 다른 스레드에서도 정상 작동 (`http-nio-8081-exec-3`)
- ✅ 고유한 TraceId/SpanId로 다른 스레드명 생성
- ✅ 이니셜이 다름: `DBG` → `DEV`

### 기능 확인 완료

| 기능 | 상태 | 비고 |
|------|------|------|
| 스팬 생성 | ✅ 작동 | `>>> SPAN CREATED: true` |
| 속성 설정 | ✅ 작동 | `>>> Set attribute: userId` |
| 링크 생성 | ✅ 작동 | 이전 테스트에서 확인 |
| **스레드명 변경** | **✅ 작동** | **`26cd7480d92c2DBG6176`** |
| 스레드명 복원 | ✅ 작동 | 원래 이름으로 복원됨 |
| 고유 TraceId/SpanId | ✅ 작동 | 매 요청마다 다름 |
| 이니셜 무작위화 | ✅ 작동 | DBG, DEV 등 |

---

## 💡 배운 점

### 1. Maven 다중 모듈 프로젝트에서는 mvn install 필수

**잘못된 방법:**
```bash
cd module-a && mvn clean package
cd module-b && mvn clean package  # module-a를 의존하는 모듈
# → module-b가 로컬 리포지토리의 예전 버전을 가져옴
```

**올바른 방법:**
```bash
cd module-a && mvn clean install
cd module-b && mvn clean package
# → module-b가 최신 버전의 module-a를 가져옴
```

### 2. inline=true vs inline=false

| 측면 | inline=true | inline=false |
|------|-------------|--------------|
| **실행 방식** | Advice 코드를 bytecode에 직접 복사 | 메서드 호출 방식 |
| **성능** | 메서드 호출 오버헤드 없음 | 메서드 호출 1회 추가 |
| **소스 수정 반영** | ❌ 안 됨 (hard-coded) | ✅ 즉시 반영 |
| **디버깅** | ❌ 어려움 | ✅ 쉬움 |
| **추천 용도** | 프로덕션 (성능) | 개발 (편의성) |

### 3. 빌드 순서와 타임스탬프 확인

**빌드 순서:**
```
1. agent-commons (의존성 없음)
   → mvn clean install

2. bytebuddy-advice (agent-commons 의존)
   → mvn clean install

3. bytebuddy-agent (agent-commons, bytebuddy-advice 의존)
   → mvn clean package
```

**타임스탬프 확인:**
```bash
# JAR 파일 내의 클래스 타임스탬프 확인
unzip -l bytebuddy-agent-1.0.0.jar | grep AgentConstants

# 결과 예시
1935  2026-03-27 22:55   com/javaagent/commons/AgentConstants.class
```

### 4. ByteBuddy Helper Injection 주의사항

- 이미 agent JAR에 포함된 클래스는 다시 injection하지 않아야 함
- HELPER_CLASSES 목록에서 제외하거나 이미 로드된 클래스인지 체크해야 함
- 중복 injection 시 `LinkageError: attempted duplicate class definition` 에러 발생

### 5. Java 모듈 시스템 (--add-opens)

Java 9+에서는 리플렉션으로 내부 API 접근 시 제한이 있음:

```xml
<jvmArguments>
    --add-opens java.base/java.lang=ALL-UNNAMED
    --add-opens java.base/java.lang.reflect=ALL-UNNAMED
    --add-opens java.base/java.io=ALL-UNNAMED
</jvmArguments>
```

하지만 이번 문제의 근본 원인은 모듈 시스템 제약이 아니었습니다.

---

## 🎯 향후 개선 사항

### 1. 개발용과 프로덕션용 Advice 분리

**개발용 (inline=false):**
```java
@Advice.OnMethodEnter(inline = false)
```

**프로덕션용 (inline=true):**
```java
@Advice.OnMethodEnter(inline = true)
```

빌드 프로파일로 분리:
```xml
<profiles>
    <profile>
        <id>dev</id>
        <properties>
            <advice.inline>false</advice.inline>
        </properties>
    </profile>
    <profile>
        <id>prod</id>
        <properties>
            <advice.inline>true</advice.inline>
        </properties>
    </profile>
</profiles>
```

### 2. 빌드 스크립트 자동화

**build.sh:**
```bash
#!/bin/bash
echo "=== Java Agent Build Script ==="

echo "1. Building agent-commons..."
cd agent-commons
mvn clean install -q
cd ..

echo "2. Building bytebuddy-advice..."
cd bytebuddy-advice
mvn clean install -q
cd ..

echo "3. Building bytebuddy-agent..."
cd bytebuddy-agent
mvn clean package -q
cp target/bytebuddy-agent-1.0.0.jar ../agent-server/
cd ..

echo "4. Building agent-server..."
cd agent-server
mvn clean package -q
cd ..

echo "=== Build Complete ==="
```

### 3. CI/CD 파이프라인 설정

**Jenkins/GitLab CI 예시:**
```yaml
build:
  script:
    - mvn clean install
    - mvn clean package
    - mvn test
  artifacts:
    paths:
      - agent-server/target/bytebuddy-agent-1.0.0.jar
```

### 4. Linux 환경에서 /proc 테스트

Windows가 아니라 Linux 환경에서 테스트 필요:
```bash
# 변경된 스레드명이 /proc에서도 확인되는지 테스트
$ cat /proc/1234/task/5678/comm
26cd7480d92c2DBG6176
```

---

## 📋 체크리스트

### 구현 완료
- [x] ThreadNameHelper 클래스 구현
- [x] SpanAdvice 연동 (inline=false)
- [x] SpanHelper에 originalThreadName 필드 추가
- [x] 스레드명 변경 로직 추가
- [x] 스레드명 복원 로직 추가
- [x] AgentConstants에 ThreadNameHelper 추가
- [x] SpanAdvice를 HELPER_CLASSES에서 제거
- [x] 이미 로드된 클래스 체크 로직 추가

### 테스트 완료
- [x] ThreadName 로그 출력 확인
- [x] 실제 스레드명 변경 확인
- [x] 스레드명 복원 확인
- [x] 여러 요청에서 고유한 TraceId/SpanId 확인
- [x] 이니셜 무작위화 확인
- [ ] /proc에서 변경된 스레드명 확인 (Linux - 테스트 필요)
- [x] OpenSearch에서 TraceId/SpanId 확인

### 빌드 프로세스 확립
- [x] Maven 빌드 순서 문서화
- [x] mvn install 사용법 확립
- [x] 타임스탬프 확인 방법 확립
- [ ] 빌드 스크립트 작성 (추가 필요)
- [ ] CI/CD 파이프라인 설정 (추가 필요)

### 배포 준비
- [x] 개발 환경에서 inline=false 설정
- [ ] 프로덕션 환경 테스트
- [ ] 모니터링 가이드 작성
- [ ] 롤백 절차 준비

---

## 🔍 추가 트러블슈팅 (2026-05-04)

### 테스트 방법

#### 1. 빌드 및 서비스 시작
```bash
# 1. 의존성 모듈 설치 (순서 중요!)
cd agent-commons && mvn clean install -q
cd ../bytebuddy-advice && mvn clean install -q
cd ../bytebuddy-agent && mvn clean package -q
cd ../agent-server && mvn clean package -q

# 2. Agent Server 시작
cd agent-server
mvn spring-boot:run > /tmp/agent-server.log 2>&1 &
# 또는 백그라운드 실행
nohup mvn spring-boot:run > /tmp/agent-server.log 2>&1 &

# 3. Test1 Service 시작
cd ../test-services/test1-service
mvn spring-boot:run > /tmp/test1-service.log 2>&1 &
# 또는 백그라운드 실행
nohup mvn spring-boot:run > /tmp/test1-service.log 2>&1 &
```

#### 2. Agent Attach 및 Instrumentation
```bash
# 1. PID 찾기
netstat -ano | grep ":8081" | grep "LISTENING" | awk '{print $5}' | head -1
# 결과 예: 10572

# 2. Agent Attach (with instrumentation parameter)
curl -X POST http://localhost:8080/api/bytebuddy/attach \
  -H "Content-Type: application/json" \
  -d '{"pid": "10572", "instrumentation": "com.test.service.shared.UserIdService:setUserId:span"}'

# 결과 예:
# {"success":true,"pid":"10572","agentJar":"C:\\Users\\EUROPE\\agent\\agent-server\\bytebuddy-agent-1.0.0.jar","message":"ByteBuddy Agent attached to PID 10572"}
```

#### 3. 테스트 요청
```bash
# 방법 1: 직접 요청 (userId 없이)
curl "http://localhost:8081/test1"

# 방법 2: userId 파라미터와 함께
curl "http://localhost:8081/test1/setUserId?userId=TEST_USER_001"

# 방법 3: 여러 요청 테스트
for i in {1..3}; do
  curl "http://localhost:8081/test1" &
  sleep 1
done
wait
```

#### 4. 로그 확인
```bash
# 백그라운드 태스크 로그 확인
tail -f /tmp/test1-service.log | grep -E "ThreadName|ADVICE|SPAN"

# 또는 Maven 로그에서 확인
tail -f /tmp/test1-service.log | grep -A 10 "ThreadName"

# 핵심 로그 메시지:
# >>> ADVICE ENTER: ...
# >>> [ThreadName] Check: spanHelper=true, isTarget=true
# >>> [ThreadName] Renaming thread...
# ===============================================
# [ThreadName] RENAMED THREAD
# [ThreadName] Original Name: http-nio-8081-exec-6
# [ThreadName] New Name:      61f9e6013c0f3STG8093
# [ThreadName] TraceId:       61f9e6062ac14f0cb32113ecdf36ff94
# [ThreadName] SpanId:        13c0f344af71f0a5
# ===============================================
# <<< ADVICE EXIT
# ===============================================
# [ThreadName] RESTORED THREAD
# [ThreadName] From: 61f9e6013c0f3STG8093
# [ThreadName] To:   http-nio-8081-exec-6
# ===============================================
```

---

### 발생한 문제점 및 해결책

#### 문제 1: 포트 충돌로 인한 서비스 시작 실패
**증상:**
```
Web server failed to start. Port 8081 was already in use.
```

**원인:**
- 이전 실행 중인 프로세스가 포트를 점유 중

**해결책:**
```bash
# 1. 포트 사용 프로세스 찾기
netstat -ano | grep ":8081" | grep "LISTENING"

# 2. 프로세스 종료
taskkill //F //PID <PID>

# 3. 또는 PowerShell에서
Get-Process -Name java | Where-Object {$_.Id -eq <PID>} | Stop-Process -Force
```

---

#### 문제 2: 백그라운드 실행 시 로그 캡처 실패
**증상:**
```
# 로그 파일이 비어 있거나 최신 로그가 없음
cat /tmp/test1-service.log
# (출력 없음)
```

**원인:**
- Maven이 출력을 버퍼링함
- stderr가 즉시 flush되지 않음

**해결책:**
```bash
# 방법 1: tee 사용 (실시간 출력 + 파일 저장)
mvn spring-boot:run 2>&1 | tee /tmp/test1-service.log

# 방법 2: unbuffered 출력 (Windows에서는 작동 안 할 수 있음)
mvn spring-boot:run 2>&1 | grep --line-buffered . > /tmp/test1-service.log

# 방법 3: 백그라운드 태스크 출력 확인
# (Claude Code 사용 시)
# 백그라운드 태스크의 ID로 출력 파일 읽기
```

---

#### 문제 3: Spring `-parameters` 플래그 누락
**증상:**
```
java.lang.IllegalArgumentException: Name for argument of type [java.lang.String] not specified,
and parameter name information not available via reflection.
Ensure that the compiler uses the '-parameters' flag.
```

**원인:**
- Maven compiler plugin이 `-parameters` 플래그 없이 컴파일
- Spring이 메서드 파라미터 이름을 reflection으로 읽을 수 없음

**해결책:**
```xml
<!-- pom.xml에 추가 -->
<plugin>
    <groupId>org.apache.maven.plugins</groupId>
    <artifactId>maven-compiler-plugin</artifactId>
    <version>3.13.0</version>
    <configuration>
        <parameters>true</parameters>
    </configuration>
</plugin>
```

**또는 우회 방법:**
- 파라미터가 없는 메서드를 먼저 테스트
- `@RequestParam`의 `name` 속성 사용

---

#### 문제 4: Controller 메서드 Instrumentation 충돌
**증상:**
```json
{
  "success": false,
  "conflictType": "OPENTELEMETRY",
  "message": "CONFLICT: OpenTelemetry auto-instruments all @Controller methods",
  "recommendation": "Skip instrumentation, OpenTelemetry will cover this"
}
```

**원인:**
- OpenTelemetry Java Agent가 이미 `@Controller` 메서드를 auto-instrumentation
- 중복 instrumentation으로 인한 충돌

**해결책:**
```bash
# 1. Controller 대신 Service 클래스 instrument
# 예: UserIdService.setUserId

# 2. instrumentation 파라미터에 Service 지정
curl -X POST http://localhost:8080/api/bytebuddy/attach \
  -H "Content-Type: application/json" \
  -d '{"pid": "10572", "instrumentation": "com.test.service.shared.UserIdService:setUserId:span"}'
```

---

#### 문제 5: Advice 로그가 출력되지 않음 (stderr buffered)
**증상:**
```
# Agent는 attach되었지만 로그가 보이지 않음
[ByteBuddy] *** ADVICE APPLIED TO com.test.service.shared.UserIdService.setUserId ***
# (그 이후로는 아무 로그도 없음)
```

**원인:**
- `System.err.println()`이 buffered되어 즉시 출력되지 않음
- 백그라운드 실행 시 stdout/stderr가 리다이렉트되면 버퍼링됨

**해결책:**
```java
// SpanAdvice.java 또는 ThreadNameHelper.java에서
// 모든 출력 후에 flush 추가
System.err.println(">>> ADVICE ENTER: " + method);
System.err.flush();  // 🆕 추가

System.out.println("[ThreadName] RENAMED THREAD");
System.out.flush();  // 🆕 추가
```

또는 로그 프레임워크 사용:
```java
private static final Logger logger = LoggerFactory.getLogger(SpanAdvice.class);

logger.info(">>> ADVICE ENTER: {}", method);
// 자동으로 flush됨
```

---

#### 문제 6: PID 찾기 실패 (jps 없음)
**증상:**
```
jps: command not found
```

**해결책:**
```bash
# 방법 1: netstat 사용 (Windows/Linux 공통)
netstat -ano | grep ":8081" | grep "LISTENING" | awk '{print $5}' | head -1

# 방법 2: PowerShell (Windows)
Get-NetTCPConnection -LocalPort 8081 -State Listen |
  Select-Object -ExpandProperty OwningProcess

# 방법 3: lsof (Linux/Mac)
lsof -ti:8081

# 방법 4: Agent Server API 사용
curl "http://localhost:8080/api/arthas/pids?port=8081"
```

---

### 테스트 시나리오

#### 시나리오 1: 기본 스레드명 변경 테스트
```bash
# 목적: 스레드명이 올바르게 변경되고 복원되는지 확인

# 1. 서비스 시작
cd test-services/test1-service && mvn spring-boot:run &

# 2. Agent attach
PID=$(netstat -ano | grep ":8081" | grep "LISTENING" | awk '{print $5}' | head -1)
curl -X POST http://localhost:8080/api/bytebuddy/attach \
  -H "Content-Type: application/json" \
  -d "{\"pid\": \"$PID\", \"instrumentation\": \"com.test.service.shared.UserIdService:setUserId:span\"}"

# 3. 테스트 요청
curl "http://localhost:8081/test1"

# 4. 로그 확인
tail -100 /tmp/test1-service.log | grep "ThreadName"

# 예상 결과:
# [ThreadName] RENAMED THREAD
# [ThreadName] Original Name: http-nio-8081-exec-X
# [ThreadName] New Name:      <TraceId 7자><SpanId 6자><INITIALS><숫자>
# [ThreadName] RESTORED THREAD
```

#### 시나리오 2: 여러 요청에서 고유 ID 확인
```bash
# 목적: 각 요청마다 고유한 TraceId/SpanId가 생성되는지 확인

for i in {1..5}; do
  echo "=== Request $i ==="
  curl "http://localhost:8081/test1"
  sleep 1
done

# 로그에서 TraceId/SpanId 추출
grep "ThreadName:" /tmp/test1-service.log | grep "TraceId:" | tail -10

# 예상 결과: 모든 TraceId와 SpanId가 다름
```

#### 시나리오 3: 스레드명 복원 확인
```bash
# 목적: 메서드 종료 후 원래 스레드명으로 복원되는지 확인

# 1. 테스트 요청 전 원래 스레드명 확인
# (로그에서 http-nio-8081-exec-X 확인)

# 2. 테스트 요청
curl "http://localhost:8081/test1"

# 3. 로그 확인
grep -A 5 "RESTORED THREAD" /tmp/test1-service.log | tail -10

# 예상 결과:
# [ThreadName] From: <변경된 이름>
# [ThreadName] To:   http-nio-8081-exec-X (원래 이름)
```

---

### 성공 확인 체크리스트

테스트 완료 후 다음 항목들을 확인하세요:

#### 1. 빌드 및 배포
- [ ] `mvn clean install`로 모듈들이 로컬 리포지토리에 설치됨
- [ ] bytebuddy-agent JAR이 agent-server 디렉토리에 복사됨
- [ ] agent-server가 정상 시작됨 (포트 8080)
- [ ] test1-service가 정상 시작됨 (포트 8081)

#### 2. Agent Attach
- [ ] PID가 올바르게 식별됨
- [ ] attach 요청이 성공함 (`success: true`)
- [ ] ByteBuddyAgent 초기화 로그가 보임
- [ ] OpenTelemetry 초기화 로그가 보임
- [ ] Helper classes injection 로그가 보임

#### 3. Instrumentation
- [ ] `createSpan` 요청이 성공함
- [ ] `*** ADVICE APPLIED TO ***` 메시지가 보임
- [ ] `Transformed bytecode created` 메시지가 보임

#### 4. 런타임 테스트
- [ ] `>>> ADVICE ENTER` 메시지가 보임
- [ ] `>>> SPAN CREATED: true` 메시지가 보임
- [ ] `>>> [ThreadName] Check: spanHelper=true, isTarget=true` 메시지가 보임
- [ ] `>>> [ThreadName] Renaming thread...` 메시지가 보임

#### 5. 스레드명 변경
- [ ] `[ThreadName] RENAMED THREAD` 블록이 보임
- [ ] Original Name이 기록됨 (예: `http-nio-8081-exec-2`)
- [ ] New Name이 형식에 맞게 생성됨 (예: `61f9e6013c0f3STG8093`)
- [ ] TraceId가 출력됨 (32자 hex)
- [ ] SpanId가 출력됨 (16자 hex)

#### 6. 스레드명 복원
- [ ] `<<< ADVICE EXIT` 메시지가 보임
- [ ] `[ThreadName] RESTORED THREAD` 블록이 보임
- [ ] From이 변경된 이름임
- [ ] To가 원래 이름임

#### 7. 여러 요청 테스트
- [ ] 각 요청마다 다른 TraceId가 생성됨
- [ ] 각 요청마다 다른 SpanId가 생성됨
- [ ] 스레드명의 이니셜 부분이 무작위로 변경됨
- [ ] 모든 요청이 성공적으로 응답함

---

## 📞 관련 문서

- [ByteBuddy Advice Documentation](https://bytebuddy.net/#/tutorial)
- [Java Instrumentation Reference](https://docs.oracle.com/javase/10/docs/api/java/lang/instrument/package-summary.html)
- [OpenTelemetry Trace API](https://opentelemetry.io/docs/reference/specification/trace/api/)
- [20260402_스레드명.md](./20260402_스레드명.md) - 초기 보고서
- [20260401_링크수정.md](./20260401_링크수정.md) - 이전 작업

---

## 📝 요약

### 구현 완료
- ✅ ThreadNameHelper: 스레드명 변경 로직
- ✅ SpanAdvice 연동: 메서드 진입/퇴출 시 호출
- ✅ 스레드명 형식: `{TraceId 6자}{SpanId 6자}{이니셜 1자}{숫자 2자}` (총 15자)
- ✅ 이니셜 규칙: 원래 스레드명의 첫 번째 알파벳 (http → H, reactor → R)
- ✅ 숫자 규칙: 원래 스레드명의 마지막 숫자 추출 (exec-1 → 01, nio-10 → 10)
- ✅ 로그 출력: TraceId/SpanId 확인 가능
- ✅ 스레드명 복원: 원래 이름으로 복원

### 문제 해결
- ❌ 런타임에 소스 수정이 반영되지 않음 → ✅ **해결: mvn install 사용**
- ❌ Inline Advice가 bytecode에 hard-coded → ✅ **해결: inline=false 변경**
- ❌ Maven 로컬 리포지토리 미설치 → ✅ **해결: mvn install 사용**
- ❌ Helper injection 중복 에러 → ✅ **해결: SpanAdvice 제거, 체크 로직 추가**

### 성공 결과
```
[ThreadName] RENAMED THREAD
[ThreadName] Original Name: http-nio-8081-exec-1
[ThreadName] New Name:      a7505712f3f1H01
[ThreadName] TraceId:       a75057b0c470c073dc2e0c244d7aff77
[ThreadName] SpanId:        12f3f1d87c3a62bc
```

### 배운 점
1. **다중 모듈 Maven 프로젝트에서는 `mvn install` 필수**
   - `package`만 하면 로컬 리포지토리에 설치되지 않음
   - 의존성 모듈을 먼저 `install` 해야 함

2. **inline=true vs inline=false**
   - 개발 중: `inline=false` (디버깅 용이)
   - 프로덕션: `inline=true` (성능 최적화)

3. **빌드 순서와 타임스탬프 확인**
   - 의존성 모듈 먼저 빌드
   - JAR 파일 내의 클래스 타임스탬프로 확인

---

**작성일**: 2026-03-27
**최종 수정일**: 2026-05-04
**버전**: 1.3.0
**상태**: ✅ 구현 완료 및 테스트 성공
**다음 단계**: 프로덕션 환경 배포 준비

---

## 🔄 버전별 변경사항

### v1.3.0 (2026-05-04)
**스레드명 형식 최적화**

**변경 내용:**
- 스레드명 길이 축소: 20자 → 15자
- TraceId: 7자 → 6자
- SpanId: 6자 (그대로)
- 이니셜: 3자 → 1자
- 숫자: 4자 → 2자

**이니셜 규칙 개선:**
- 이전: 무작위 3자리 (SRV, API, WEB, DBG 등)
- 현재: 원래 스레드명의 첫 번째 알파벳 1자리
  - `http-nio-8081-exec-1` → **H** (http)
  - `reactor-http-nio-10` → **R** (reactor)
  - `pool-1-thread-3` → **P** (pool)

**숫자 추출 개선:**
- 이전: 무작위 4자리 (0000-9999)
- 현재: 원래 스레드명의 마지막 숫자 2자리
  - `http-nio-8081-exec-1` → **01**
  - `reactor-http-nio-10` → **10**

**새로운 형식:**
```
{TraceId 6자}{SpanId 6자}{이니셜 1자}{숫자 2자}
예: a7505712f3f1H01
   ------^^^^^^^^-^----
   Trace  Span   INu
   6자    6자    이 2
                 니
                 자
                 숫
                                        자
```

### v1.2.0 (2026-05-04 초기)
- 트러블슈팅 가이드 추가
- 테스트 시나리오 추가
- 성공 확인 체크리스트 추가

### v1.1.0 (2026-03-27)
- 초기 구현 완료
- 기본 스레드명 형식: {TraceId 7자}{SpanId 6자}{이니셜 3자}{숫자 4자}

---

## 🎉 최종 테스트 결과 (2026-05-04)

### 테스트 환경
- OS: Windows 11 Pro
- Java: 21.0.9
- Maven: 3.x
- Spring Boot: 3.2.5
- OpenTelemetry Java Agent: 2.5.0

### 테스트 결과 (2026-05-04 업데이트)

```
>>> ADVICE ENTER: public java.lang.String com.test.service.shared.UserIdService.setUserId(java.lang.String)
[SpanHelper] Span created and made current: public java.lang.String com.test.service.shared.UserIdService.setUserId(java.lang.String)
>>> SPAN CREATED: true
>>> [ThreadName] Check: spanHelper=true, isTarget=true
>>> [ThreadName] Current thread: http-nio-8081-exec-1
>>> [ThreadName] Renaming thread...
===============================================
[ThreadName] RENAMED THREAD
[ThreadName] Original Name: http-nio-8081-exec-1
[ThreadName] New Name:      a7505712f3f1H01
[ThreadName] TraceId:       a75057b0c470c073dc2e0c244d7aff77
[ThreadName] SpanId:        12f3f1d87c3a62bc
===============================================
>>> DEBUG: Looking up key: com.test.service.shared.UserIdService.setUserId
>>> DEBUG: userId=null
>>> DEBUG: links=0
>>> DEBUG: paramMapping=null
<<< ADVICE EXIT
<<< SPAN COMPLETED
===============================================
[ThreadName] RESTORED THREAD
[ThreadName] From: a7505712f3f1H01
[ThreadName] To:   http-nio-8081-exec-1
===============================================
```

### 검증 완료 항목 (2026-05-04 업데이트)
- ✅ 스레드명 변경: `http-nio-8081-exec-1` → `a7505712f3f1H01`
- ✅ TraceId 추출: `a75057b0c470c073dc2e0c244d7aff77` (앞 6자: `a75057`)
- ✅ SpanId 추출: `12f3f1d87c3a62bc` (앞 6자: `12f3f1`)
- ✅ 이니셜 추출: `H` (http-nio-8081-exec-**h**ttp → **H**)
- ✅ 숫자 추출: `01` (http-nio-8081-exec-**1** → **01**)
- ✅ 스레드명 형식: `{TraceId 6자}{SpanId 6자}{이니셜 1자}{숫자 2자}` = 15자
- ✅ 스레드명 복원: 원래 이름으로 복원됨
- ✅ ThreadNameHelper injection 성공
- ✅ SpanAdvice 적용 성공

### 다양한 스레드명 예시

| 원래 스레드명 | 변경된 스레드명 (예시) | 설명 |
|---------------|----------------------|------|
| `http-nio-8081-exec-1` | `a7505712f3f1H01` | http → H, exec-1 → 01 |
| `http-nio-8081-exec-2` | `cf1f5a5aa57bH02` | http → H, exec-2 → 02 |
| `reactor-http-nio-10` | `61f9e6013c0fR10` | reactor → R, nio-10 → 10 |
| `pool-1-thread-3` | `26cd7480d92cP03` | pool → P, thread-3 → 03 |

### 배운 점
1. **mvn install 사용**: 의존성 모듈은 먼저 `install`해야 함
2. **백그라운드 실행 로그 캡처**: `tee` 또는 백그라운드 태스크 출력 확인
3. **Controller 충돌 회피**: Service 클래스를 instrument하여 OpenTelemetry 충돌 방지
4. **stderr flush**: `System.err.flush()`로 로그가 즉시 출력되도록 함
